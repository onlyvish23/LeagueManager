@model IEnumerable<LeagueManager.Models.CountryC>
@{
    ViewBag.Title = "CountryList";
}
<h2>
    CountryList</h2>
<script src="../../Scripts/jquery-1.7.1.min.js" type="text/javascript"></script>
<script type="text/javascript">
//    if (TempData["Data"] != null) {
//        alert("Country inserted successfully into the system.");
//    }
</script>
@if (TempData["Data"] != null)
{ 
    <b>Data inserted successfully into the system</b>   
}
@if (TempData["ErrorMsg"] != null)
{ 
    <b>@TempData["ErrorMsg"]</b> 
    @Html.Raw(@TempData["ErrorMsg"])  
}
    
    
    
@* if(TempData["Data"]!=null)
   {
      <script type="text/javascript">
          alert('Added successfully');
      </script>
   }*@
<p>
    @Html.ActionLink("Create New", "AddCountry")
    <br />
    @Html.ActionLink("Create Country By Form", "InsertCountry");
</p>
<div>
    <input type="button" value="Go To Add Country" onclick="location.href='@Url.Action("DeleteSelectedCountry", "League")'" />
</div>
@using (Html.BeginForm("DeleteSelectedCountry", "League", FormMethod.Post))
{
    
    @Html.ActionLink("Delete Selected Country", "DeleteSelectedCountry");
    <input type="submit" value="Hit" />
    <table>
        <tr>
            <th>
                Select
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CountryID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CountryName)
            </th>
            <th>
            </th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @* <input type="checkbox" value="@item.CountryID" name="deleteList" id="deleteList" />*@
                    @Html.CheckBox(item.CountryID.ToString())
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CountryID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CountryName)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Details", "Details", new { /*id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.CountryID })
                </td>
            </tr>
        }
    </table>
}


